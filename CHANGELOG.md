# 3.4-beta1 (01.01.2020)

* Fixed "factory uses unchecked or unsafe operations" warning message.
* New auto-type detection for types extending just `Object` / `Any`.
* Multiple improvements to error messages and code. 